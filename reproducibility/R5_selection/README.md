# R5 — HepMC-to-selection cutflow

**Question.** Can the release rebuild the compact seven-event dataframe directly
from the version-controlled HepMC2 input and reproduce the documented ANUBIS
selection outcomes?

The generated output contains the regenerated dataframe, the deterministic
summary and standalone JSON/HTML trace reports. The sample includes events that
stop at different cutflow stages and one event that reaches `Final`.
