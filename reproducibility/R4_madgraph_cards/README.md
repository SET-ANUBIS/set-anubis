# R4 — MadGraph input-card preparation

**Question.** Does the release generate the same process commands, run card,
parameter card, Pythia8 shower card and MadSpin decay card?

MadGraph and Docker are not launched. The generated text files are validated by
SHA-256 digests recorded in the expected summary.
