# R2 — Branching ratios and decay widths

**Question.** Does the branching-ratio layer reproduce the reference partial
widths, total width and branching fractions from a version-controlled table?

The scenario deliberately does not run MARTY or MadGraph. It validates the
release's deterministic interpolation and aggregation logic.
