# R3 — Pythia command preparation

**Question.** Does the release create the same generic Pythia `.cmnd` input for
a fixed particle identifier?

This scenario tests card preparation only; it does not require the optional
native Pythia8/HepMC3 extension.
