# MadGraph development inputs

Place local MadGraph cards and `jobscript_param_scan.txt` here, or point the
helper to another directory with `SETANUBIS_MADGRAPH_INPUT_DIR`.

Generated event samples should not be committed to the repository.
